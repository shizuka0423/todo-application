import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';
import 'package:flutter_todo_application/presentation/theme/sizes.dart';

class AppIcons {
  static const star = Icon(Icons.star_border);
  static const tag = Icon(Icons.label_outline);
  static const eventStart = Icon(Icons.event_outlined);
  static const eventEnd = Icon(Icons.event_available_outlined);
  static const notice = Icon(Icons.notifications_outlined);
  static const repeat = Icon(Icons.autorenew_sharp);
  static const check = Icon(Icons.check_rounded);
  static const upDown = Icon(Icons.unfold_more, size: RawSize.p18);
  static const next = Icon(Icons.arrow_forward_ios_rounded);
  static const menu = Icon(Icons.menu);
  static const search = Icon(Icons.search, size: 24);
  static const sort = Icon(CupertinoIcons.slider_horizontal_3);
  static const flag = Icon(Icons.flag_outlined);
  static const cross = Icon(Icons.close_sharp);
  static const pallet = Icon(Icons.color_lens_outlined);
  static const add = Icon(Icons.add_rounded);
}

class IconDatas {
  static const tag = Icons.label_outline;
  static const repeat = Icons.autorenew_sharp;
  static const notice = Icons.notifications_outlined;
  static const circle = Icons.radio_button_unchecked;
  static const checkCircle = Icons.check_circle_outline_outlined;
  static const cross = Icons.close_sharp;
  static const delete = Icons.delete_outline_sharp;
  static const add = Icons.add_sharp;
  static const next = Icons.keyboard_arrow_right_rounded;
  static const dropDown = Icons.keyboard_arrow_down_rounded;
}
