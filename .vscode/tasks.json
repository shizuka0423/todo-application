{
    "version": "2.0.0",
    "tasks": [
        /*{
            "label": "open_ios_sim",
            "type": "shell",
            "command": "open -a Simulator --args -CurrentDeviceUDID ${config:ios.simulator}"
        },
        {
            "label": "open_android_emu",
            "type": "shell",
            "command": "flutter run -d ${config:android.emulator}"
        }*/
        {
            "label": "open_android_emu",
            "type": "shell",
            "command": "flutter run"
        }
    ]
}